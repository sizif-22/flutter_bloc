import 'package:flutter/material.dart';

class CharactersScreen extends StatelessWidget {
  const new({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Characters Screen');
  }
}
